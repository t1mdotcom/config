
# sudo
alias _="sudo"
alias fucking="sudo"
alias damn="sudo !!"

# docker
alias cocker="docker compose"

# general
alias q=exit
alias threads='ps -eo nlwp | tail -n +2 | awk "{ num_threads += $1 } END { print num_threads }"'